# Dependencies for Render / production deploy.
# ROUTE: pip install → webapp/server.py (FastAPI + uvicorn)
# NEXT: render.yaml startCommand runs uvicorn webapp.server:app
phonenumbers>=8.13.0
pdfplumber>=0.11.0
python-docx>=1.1.0
rapidfuzz>=3.6.0

# Web app (Render / production)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
